import { normalize } from "./utils";

const fontScale = {
    text : normalize(12),
    title : normalize(14),
    button :  normalize(16),
    headerText : normalize(16),
    creditTxt : normalize(35)
}
export default fontScale;