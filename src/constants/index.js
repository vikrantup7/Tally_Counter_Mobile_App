import Colors from './colors';
import Fonts from './fonts';
import fontScale from './fontScale';
import {showToastMessage, normalize, ValidationRegex, height, width} from './utils';

export { Colors, Fonts, fontScale, showToastMessage, normalize, ValidationRegex, height, width};
