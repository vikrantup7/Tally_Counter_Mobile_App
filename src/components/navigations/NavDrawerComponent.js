import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const NavDrawerComponent = ({navigation}) => {
  return (
    <View>
      <Text>NavDrawerComponent</Text>
    </View>
  )
}

export default NavDrawerComponent

const styles = StyleSheet.create({})